#!/usr/bin/env bash
# SCOPE: both
# Compatibility wrapper for the canonical snake_case Python entrypoint.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/harness_parity_audit.py" "$@"
