#!/usr/bin/env bash
# SCOPE: os-only
# PURPOSE: Provision or dry-run an operational Cognitive OS instance profile.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/cos_instance_init.py" "$@"
