#!/usr/bin/env bash
# SCOPE: os-only
# PURPOSE: Record proof-drill evidence rows for the ACC proof_drill_evidence adapter.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/proof_drill_evidence_record.py" "$@"
