#!/usr/bin/env bash
# SCOPE: os-only
# Wrapper for the ADR-091 headless protected-publication checker.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "${SCRIPT_DIR}/cos_headless_publication.py" "$@"
