#!/usr/bin/env bash
# SCOPE: os-only
# PURPOSE: Submit a Phase 1 local COS service-control-plane task.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/cos_service_control_plane.py" submit "$@"

