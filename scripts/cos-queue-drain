#!/usr/bin/env bash
# SCOPE: os-only
# PURPOSE: Report Phase 1 local COS service-control-plane queue state.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/cos_service_control_plane.py" queue-drain "$@"

