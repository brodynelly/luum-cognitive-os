# Advisor MCP Server — Dependencies
# Core (required)
fastmcp>=2.0.0

# Provider SDKs (install only the ones you use)
anthropic>=0.40.0       # provider="anthropic"
openai>=1.50.0          # provider="openai"
google-generativeai>=0.8.0  # provider="google"
httpx>=0.27.0           # provider="local" (Ollama)
