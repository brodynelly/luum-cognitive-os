#!/usr/bin/env bash
# SCOPE: both
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [ -f "$SCRIPT_DIR/cos_quality_duplicates.py" ]; then
  exec python3 "$SCRIPT_DIR/cos_quality_duplicates.py" "$@"
fi
exec python3 "$SCRIPT_DIR/../bin/cos_quality_duplicates.py" "$@"
