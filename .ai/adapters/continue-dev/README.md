# Continue.dev portable `.ai` adapter

Schema: `portable-ai-adapter.v1`

This adapter is generated from Cognitive OS canonical primitive manifests.
It must not invent primitive behavior or overclaim runtime enforcement.
It is declarative: native IDE file emission is delegated to governed harness projection drivers through the adapter compiler.

## Current projection

- harness id: `continue-dev`
- status: `implemented`
- proof level: `structural`
- projection mode: `continue-rules-mcpservers`

## Settings paths

- `.continue/rules/cognitive-os.md`
- `.continue/mcpServers/cognitive-os.json`

## Rule

Read `.ai/profiles/continue-dev.json` for declared fidelity before projecting primitives into this host.
Structural advisory surfaces are not runtime enforcement.
