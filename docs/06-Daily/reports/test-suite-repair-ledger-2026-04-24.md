# Test Suite Repair Ledger — 2026-04-24

This ledger tracks full-suite repair decisions so future sessions do not lose the historical context behind each changed test or runtime fix.

## Policy

A passing test is not automatically trusted. Every test family touched during the repair pass must be classified against repository history:

- `active-contract`: the test protects current product behavior and failures should usually be fixed in runtime.
- `stale-contract`: the test protects behavior replaced by a later ADR, commit, or documented architecture decision.
- `optional-lane`: the test requires external services, credentials, packages, or platform capabilities not present in the default local lane.
- `false-positive-risk`: the test can pass while checking only structure, stale assumptions, or a weak proxy for real behavior.

## Current Evidence

- Latest full run: `102 failed, 10735 passed, 1283 skipped, 18 xfailed`.
- Previous comparison point during the same repair effort: `177 failed, 10625 passed, 1281 skipped, 18 xfailed`.
- Command: `python3 -m pytest tests/ -n auto -q --tb=short -ra --disable-warnings --timeout=120 --timeout-method=thread --session-timeout=2400 --durations=25`.
- Log: `.cognitive-os/reports/pytest-full-latest.log`.
- Focused repair lanes after canonical projection work:
  - orphan/registration subset: `46 passed`
  - native heartbeat integration: `4 passed`
  - usage-health + quick tracker subset: `2 passed`
  - metrics rotation: `5 passed`
  - cos-index: `13 passed`
  - agent-bus smart-infra subset: `6 passed`
  - orchestrator capabilities live: `9 passed`

## Decisions So Far

| Family | Historical evidence | Classification | Decision | Validation |
|---|---|---|---|---|
| Telemetry behavior tests | `8e943b7` migrated `lib/telemetry.py` to `MetricEvent` and promised consumers remain flat via unwrap. | stale-contract + runtime bug | Keep MetricEvent, update tests to unwrap, and make `_append()` return `None` if `append_event()` fails. | `tests/behavior/test_telemetry.py tests/unit/test_metric_event_migration.py`: 30 passed. |
| Efficiency profiles | `6c5d810` and ADR-002 collapsed legacy `lean/standard/minimal` into `default/full`; `cognitive-os.yaml` documents `default | full`. | stale-contract | Update tests from `lean/standard/full` to `default/full`; preserve legacy `lean` only as old-project compatibility. | `tests/unit/test_efficiency_stress.py tests/behavior/test_efficiency_profiles.py tests/integration/test_consolidation_external.py`: 58 passed. |
| Rules source vs patterns | `c0db698` moved declarative rules to `docs/04-Concepts/patterns`; `bb5e962` made skills/rules canonical-first. | stale-contract | Tests may resolve compact/contextual references to either enforceable `rules/` or documented `docs/04-Concepts/patterns/`. Do not recreate deliberately moved rules. | Included in prior audit target and efficiency/consolidation runs. |
| Data pipeline / feedback loops / self-repair JSONL reads | `8e943b7` migrated `consequence_engine`, `skill_archive`, `learning_pipeline`, and `record_completion` writers to `MetricEvent`. | stale-contract + false-positive-risk | Add shared `tests.utils.jsonl` reader that flattens MetricEvent rows and preserves consequence `record_type`. | `tests/integration/test_data_pipeline.py tests/integration/test_feedback_loops.py tests/integration/test_e2e_self_repair.py`: 74 passed. |
| Skill frontmatter with SCOPE tags | `5acb797` added `<!-- SCOPE: ... -->` before skill frontmatter; `95e182d` migrated skill frontmatter. | stale-contract | Tests must parse optional leading SCOPE comments before YAML frontmatter. | `tests/behavior/test_code_review_skill.py tests/behavior/test_semgrep_integration.py::TestSemgrepSkillFile tests/behavior/test_pentest_self.py::TestPentestSelfSkill`: 35 passed. |
| Subagent context injection | `931aaef` introduced mandatory sub-agent context; `6e2cf8b` added mandatory rules; current hook emits Claude modern `hookSpecificOutput.additionalContext`. | stale-contract + active bug | Tests read the current nested output shape; runtime now exits 0 for invalid JSON before jq-dependent parsing can trip `set -e`. | `tests/hooks/test_subagent_context_injector.py`: 14 passed. |
| Auto-update/cos-init settings driver fixtures | `e5fe9ad` introduced harness-aware settings projection; `7821f73` shared harness autodetection in init. Fake COS sources that copied scripts without `scripts/_lib/settings-driver.sh` no longer represented a runnable release tree. | stale-fixture + active-contract | Test fixtures must copy the settings-driver helper; `cos-update.sh` settings regeneration must not explode under isolated function tests when settings-driver globals are absent. | `tests/integration/test_auto_update_flow.py tests/integration/test_auto_update_safety.py tests/behavior/test_cos_update.py tests/unit/test_cos_update_regenerate_settings.py`: 56 passed. |
| Canonical projection behavior | ADR-057 and `.cognitive-os/plans/architecture/skills-rules-canonicalization-workplan.md` define `.cognitive-os/` as source-of-truth and `.claude/.codex` as driver projections. Structural tests could pass while `cos-init.sh` only wrote rules to `.claude/rules/cos`, duplicated skill content in the driver projection, and generated settings could reference hooks excluded by scope. | false-positive-risk + active bug | Add behavioral install contracts that verify full skill/rule projection to canonical storage, runtime discovery without `.claude/skills`, and Codex hook commands pointing at installed canonical hooks. Update `cos-init.sh` to dual-write rules, preserve `--full`, filter skills by audience, project Claude skills as flat symlinks, and clean canonical rule projection on auto-update. | `tests/contracts/test_canonical_projection_behavior.py tests/integration/test_project_settings_generation.py tests/integration/test_auto_update_safety.py::TestCosInitNamespacing::test_cos_init_installs_to_cos_namespace tests/integration/test_install_scope.py`: 35 passed. Auto-update regression lane: 56 passed. |
| Behavioral test doctrine | Current repair work found green tests that checked shape without proving runtime effect. | false-positive-risk | Add a reusable testing doctrine so future tests prove installation, projection, discovery, execution, metrics, or safety effects instead of relying on file-existence proxies alone. Operationalize it as a reusable OS skill so future sessions do not depend on conversational memory. | `docs/04-Concepts/architecture/behavioral-test-contracts.md` added; `skills/test-contract-repair/SKILL.md` operationalizes the workflow; enforcement grows through contract tests. |
| Claude settings projection drift | `settings.json.bak-before-regen` proved the repository previously projected a wider Claude default hook surface. A later regeneration collapsed `scripts/apply-efficiency-profile.sh default`, dropping real hooks such as `docker-drift-detector.sh`, `session-startup-protocol.sh`, `session-heartbeat.sh`, `native-agent-heartbeat.sh`, `rate-limit-detector.sh`, `work-queue-sync.sh`, and stop/audit hooks. That created false orphan-hook failures plus multiple registration regressions. | active-contract + runtime bug | Restore the lost default projection from the historical baseline, keep the remaining truly opt-in ADR-056 quota hooks and compatibility heartbeat aliases explicitly whitelisted, and regenerate committed `.claude/settings.json` from the repaired script so the installer and repo baseline converge again. | `tests/contracts/test_orphan_hooks.py tests/unit/test_docker_drift_detector.py tests/unit/test_rate_limit_detector.py tests/unit/test_startup_protocol.py tests/unit/test_work_queue_sync.py tests/unit/test_surface_fix_detector.py`: 46 passed. `tests/integration/test_native_agent_heartbeat.py`: 4 passed. |
| Cost prediction discoverability | Routing/tests treated `cost-predict` as a public command alias, but the repository had no real skill implementation behind it. | false-positive-risk + active bug | Materialize the command as a real skill (`skills/cost-predictor/SKILL.md`) and a runnable CLI wrapper (`scripts/cost_predict.py`) backed by `lib.cost_predictor.py`, then re-register it in `skills/CATALOG.md`. | `tests/behavior/test_skill_auto_selection.py::TestSkillsExistOnFilesystem::test_every_routing_skill_is_known tests/unit/test_skill_router.py::TestRoutingTableIntegrity::test_all_routing_entries_have_existing_skills`: 2 passed. CLI smoke output validated locally. |
| Package integrity drift | `packages/cos-self-knowledge/` had runtime code but no top-level package documentation, so integrity tests correctly treated it as an undocumented stub. | active-contract | Add package README describing purpose, contents, and ADR reference so the package is both discoverable and auditable. | `tests/audit/test_integrity.py::test_every_package_has_readme_or_skill_md[cos-self-knowledge]`: 1 passed. |
| Usage health startup timeout | `hooks/usage-health-check.sh` called `ComponentUsageTracker.generate_usage_report()` from SessionStart. That code scans lib imports across the full repo and was taking long enough to trip multiple hook timeout tests. | active-contract + runtime bug | Keep the full report for explicit analysis, but add `generate_quick_health_report()` for startup use and switch the hook to the fast path. The hook now behaves like a real SessionStart advisory instead of a full dead-weight audit. | `tests/behavior/test_usage_health_check.py tests/hooks/test_hook_graceful_degradation.py -k usage-health-check`: 2 passed. Manual runtime check: ~25 ms. |
| cos-index generation drift | `packages/cos-index/index/packages.yaml` missed `@cos/advisory-llm`, and the generator's bash parser produced invalid YAML when `keywords:` used multiline bullets. | active-contract + runtime bug | Fix the generator to parse bullet-list keywords with portable whitespace handling, regenerate the index, and validate it from source instead of hand-editing only the output. | `packages/cos-index/scripts/generate-index.sh && validate-index.sh` passed; `tests/behavior/test_cos_index_and_global_init.py`: 13 passed. |
| Metrics rotation archive path | `hooks/metrics-rotation.sh` wrote archives to `metrics/archive/` while the active integration contract expected `.cognitive-os/metrics/.archive/`. Archive creation and retention cleanup therefore appeared broken to the tests. | active-contract + runtime bug | Align the hook to `.archive/` for the current contract and migrate legacy `archive/` gzip files into the active location so existing data is not stranded. | `tests/integration/test_metrics_rotation.py`: 5 passed. |
| Agent bus smart-infra tests vs ADR-042 | ADR-042 changed Valkey resolution order to `primary -> local daemon fallback -> smart_infra`, but the smart-infra unit tests still modeled “first failure goes straight to Docker start.” | stale-contract | Update the tests to fail both pre-smart-infra probes before asserting the `ensure_service('valkey')` path. Preserve the runtime's daemon-first behavior. | `tests/unit/test_agent_bus.py -k SmartInfraIntegration`: 6 passed. |
| Executor capability live check | `OrchestratorCapabilities._check_executor()` now accepts either `ORCHESTRATOR_MODE=executor` or a live executor daemon marker in `.cognitive-os/runtime/`, but the integration test still asserted env-var-only behavior. | stale-contract | Update the live integration test to reflect the current runtime contract: env var OR a live daemon state file with an alive PID. | `tests/integration/test_orchestrator_capabilities_live.py`: 9 passed. |
| Self-install performance + agent-team projection | `hooks/self-install.sh` still met the architectural contract but spent ~7.2s creating ~225 symlinks in the performance scenario because relative-link computation launched expensive path resolution per link. At the same time, `scripts/apply-efficiency-profile.sh default` had dropped `TeammateIdle`, `TaskCreated`, and `TaskCompleted` from the committed Claude projection. | active-contract + runtime bug | Keep the performance contract, replace per-link path resolution with a shell-native relative path builder, restore agent-team events to the default Claude projection, and regenerate `.claude/settings.json` from source. | `tests/behavior/test_self_install.py tests/hooks/test_agent_teams_hooks.py`: 52 passed. Manual repro dropped from ~7.2s to ~1.2s for the 100-component case. |
| Safety mesh blast-radius + pre-commit gate drift | `docs/06-Daily/reports/sub-agent-context-trim-2026-04-20.md` (commit `2264356`) raised blast-radius thresholds and switched advisory output to compact `hookSpecificOutput.additionalContext` JSON. Several tests still expected pre-trim `CRITICAL` semantics for any infra/security keyword and plain stdout strings. Separately, `hooks/pre-commit-gate.sh` had already removed inline `pytest` per ADR-028 D4, but legacy tests still treated “pytest inside pre-commit” as the contract. During revalidation we also found one real heuristic gap: broad prompts like `all API endpoints` were not matching the new cross-service regex. | stale-contract + runtime bug | Update the tests to assert the current contract (compact additionalContext output or canonical metrics when silent, and structural-only pre-commit behavior), while fixing the real regex gap in `blast-radius.sh` so broad `all API endpoints` prompts still surface as advisory HIGH/CRITICAL. | `tests/behavior/test_safety_mesh.py tests/behavior/test_safety_mesh_integration.py tests/hooks/test_hook_basics.py -k 'BlastRadius or PreCommitGate or security_keywords_escalate'`: 27 passed. |
| Qwen provider config detection | `86a543f` added `_load_dotenv_once()` so `lib/qwen_provider.py` now treats repo-root `.env` as part of the effective provider configuration surface. The failing tests still modeled “process env only,” so they went red on developer machines where `.env` already contained `ALIBABA_QWEN_*`. | stale-contract | Keep `.env` autoload support and isolate the negative-path tests by explicitly disabling dotenv loading (`_COS_QWEN_DOTENV_LOADED=1`) when asserting “not configured” behavior. | `tests/unit/test_qwen_provider.py`: 36 passed. |
| Manifest check bootstrap portability | `tests/integration/test_manifest_e2e.py` intentionally constrains `PATH` to only fake tools plus `bash`/`python3`. `scripts/manifest-check.sh` still depended on external `dirname` during its own bootstrap, so it failed before reaching manifest loading and then surfaced a misleading `ModuleNotFoundError` for `lib`. | active-contract + runtime bug | Remove the external `dirname` dependency from the bootstrap path and resolve `SCRIPT_DIR` using shell parameter expansion only. Keep the manifest loader import path rooted from the real repo after the script cd's into `REPO_ROOT`. | `tests/integration/test_manifest_e2e.py`: passed in the combined lane below. |
| Coverage report performance + release canary upgrade contract | `tests/coverage-report.sh` took ~37.8s because it spawned hundreds of `grep/sed/xargs` subprocesses, tripping the 30s test timeout even though the report itself was valid. Separately, `scripts/cos-release-check.sh` had a broken `upgrade_idempotent` path: it called `hash_canary_skills_surface` before definition and emitted Python-invalid lowercase `true/false`, which collapsed the scenario details into an empty string. | active-contract + runtime bug | Rewrite the coverage report scan as a single Python pass over the test corpus while preserving the same report surface, and repair `cos-release-check.sh` so the helper is defined before use and the JSON-building heredoc uses valid Python booleans. | `tests/test_coverage_report.py tests/integration/test_fresh_install_canary.py::TestFreshInstallCanary::test_upgrade_is_idempotent`: 4 passed. Manual runtime: `bash tests/coverage-report.sh` dropped from ~37.8s to ~23.5s. |
| Rate-limit retry loop under self-hosting | D45 promised end-to-end retry propagation (`rate-limiter.sh` → queue → `rate-limit-drain.sh` / `rate-limit-precheck.sh`), but the hooks were importing `lib.rate_limiter` from the project root instead of the OS root, `rate-limiter.sh` was not persisting blocked bash commands or command hashes, and `rate-limit-precheck.sh` still read only the legacy JSON queue instead of the live JSONL-backed queue state. In external/self-hosted projects that meant the drainer exited silently, precheck could not lift retries, and direct re-execution never had the original command available. | active-contract + runtime bug | Import the runtime library from the hook root, not the target project; persist blocked bash command + `command_hash` into queue context; make `rate-limit-precheck.sh` resolve queue state through `RateLimitQueue` so JSONL and migrated legacy queues behave the same; keep the drainer/project paths separate so self-hosting remains portable. | `tests/integration/test_rate_limiter_hook_retry_flow.py tests/integration/test_rate_limit_precheck_lift.py`: 15 passed. |
| Profile/config/compose drift after ADR-049 + package symlinks | The active architecture had moved `promptfoo` out of live `security.*`, removed `litellm` from Docker Compose in favor of direct SDK/process paths, and kept `paranoid` as a superset of `standard`, but several tests still asserted the old surfaces. In parallel, `lib/orchestrator_mode.py` is a symlink into `packages/agent-lifecycle/`, so resolving `cognitive-os.yaml` with a fixed `parents[1]` path skipped the repo root and lost the explicit `sdd.archive -> haiku` contract. | stale-contract + active-contract | Update tests to assert the current architecture (no dead `security.promptfoo`, no `litellm` service expectation), restore the two missing paranoid hooks (`reinvention-check.sh`, `skill-usage-tracker.sh`), and make `orchestrator_mode` walk upward until it finds the real repo-level `cognitive-os.yaml`. | `tests/behavior/test_hook_architecture.py tests/behavior/test_security_integrations.py tests/integration/test_service_health.py tests/unit/test_orchestrator_mode.py`: covered by the 76-pass focused lane below. |
| Embeddings tests and mutable settings baseline | `tests/unit/test_reinvention_embeddings.py` claimed to mock the optional embeddings stack, but still required real `numpy`, so the suite skipped/fail-oscillated depending on local extras instead of validating behavior. Separately, the profile tests and hook-architecture tests both touched `.claude/settings.json`, causing order-dependent failures when run together. | false-positive-risk + stale-fixture | Replace the test-only `numpy` dependency with a tiny fake array backend that exercises the persistence/query contract without the semantic extra installed, and harden the settings tests so hook-architecture restores `.claude/settings.json` after profile generation while efficiency-profile assertions compare against the current repo baseline on disk. | `tests/unit/test_reinvention_embeddings.py`: 7 passed. Focused combined lane: `tests/behavior/test_efficiency_profiles.py tests/behavior/test_hook_architecture.py tests/behavior/test_security_integrations.py tests/integration/test_service_health.py tests/unit/test_orchestrator_mode.py tests/unit/test_reinvention_embeddings.py tests/integration/test_rate_limiter_hook_retry_flow.py tests/integration/test_rate_limit_precheck_lift.py`: 76 passed, 2 skipped (services down). |
| Service health semantics for optional/reference stacks | `cognitive-os.yaml` classifies `opik` as `cloud`, `cognee` as `pip`, and `langfuse` as `disabled`, while `docker-compose.cognitive-os.yml` still keeps reference stacks for migration/isolated validation. The old `test_service_health.py` mixed those layers and made localhost probes look like default-lane product expectations. | stale-contract + optional-lane | Reclassify the test as a compose/runtime contract: assert runtime modes from `cognitive-os.yaml`, assert reference services remain defined in compose under the correct profiles, and probe localhost only when the developer explicitly started that stack. Keep real stack boot/traffic proof in the `testcontainers` lanes. | `tests/integration/test_service_health.py`: validated in the focused infrastructure lane below. |
| Opik/Cognee reference stack drift | The reference stack still assumed older upstream startup contracts: ClickHouse readiness by fragile log line, Opik backend envs that no longer matched upstream expectations (split DB fields + Valkey compatibility env), and Cognee startup via `python:3.13-slim` plus `python -m cognee.api.server`, even though current docs require Python 3.9–3.12 and `uvicorn cognee.api.client:app`. Those mismatches made the optional `testcontainers` lane fail for reasons unrelated to the product's own logic. | active-contract + optional-lane | Keep Valkey as the only backend cache/queue, pass provider-compatibility env names only where required (`REDIS_URL` pointing at Valkey), switch ClickHouse readiness to `/ping`, update Opik stack wiring in compose/tests to the current backend contract, and start Cognee with the current API entrypoint on Python 3.12. | `tests/integration/test_e2e_flows.py::TestObservabilityFlow::test_opik_is_alive`: passed. `tests/integration/test_app_services.py::TestOpikBackendService::test_opik_is_alive`: passed. `tests/integration/test_platform_services.py::TestCogneeServerService::test_cognee_server_health`: passed. `tests/integration/test_e2e_flows.py::TestMemoryFlow::test_cognee_health`: passed. |
| Persistent pytest evidence | Repair sessions were losing detail from partial, interrupted, or broad pytest runs. That made later sessions re-run suites just to recover summaries, and it weakened the doctrine that every test repair must leave inspectable evidence. | active-contract + process bug | Add `scripts/pytest-with-summary.sh`, document it as the default Cognitive OS maintainer runner for repair-oriented pytest runs, and bind it to the OS-only `test-contract-repair` skill. Add `scripts/test_run_inventory.py` so every persisted run also produces `inventory.md`/`inventory.json` with failures, errors, skips, xfails, slow tests, and heuristic tags (`optional-lane`, `drift`, `aspirational`, `timeout`, `false-positive-risk`). Keep the runner internal to the OS maintenance surface; adopter projects keep their own test commands unless they opt into OS maintainer tooling. | `scripts/pytest-with-summary.sh` syntax check passed. `tests/unit/test_test_run_inventory.py tests/unit/test_catalog_loading.py tests/audit/test_skills_contracts.py`: passed in focused lanes. |
| Canonical projection timeout budget | The canonical projection tests execute full `cos-init --full` installs to prove skills/rules/hooks project into `.cognitive-os/` and driver views. The tests already modeled 120-second subprocess budgets in adjacent install-scope coverage, but the root SIGALRM fixture still killed every test at 30 seconds, making valid behavioral contracts fail before they could complete. | active-contract + test harness bug | Keep the hard SIGALRM guard, register `contract` and `timeout(seconds)` markers, teach the autouse timeout fixture to honor explicit timeout markers, and mark the full canonical projection contract lane with a 120-second budget. This preserves hang protection without weakening the install/projection contract. | `tests/contracts/test_canonical_projection_behavior.py tests/integration/test_install_scope.py`: 8 passed via `scripts/pytest-with-summary.sh`. |
| Optional app-service reference stack in full-suite lane | A broad `tests/` run reached `tests/integration/test_app_services.py::test_opik_is_alive` and timed out while waiting for a heavy Opik testcontainers stack. The infrastructure catalog classifies Opik/Langfuse-style local stacks as optional/reference, not default product requirements. Letting this lane run by default turns optional service availability into a false core failure. | optional-lane + test harness bug | Gate `test_app_services.py` behind `COS_RUN_OPTIONAL_APP_SERVICES=1`, keep the tests runnable as an explicit Docker reference lane, and make `test-run-inventory.py` produce a synthetic timeout item when pytest dies before JUnit XML exists. | `tests/unit/test_test_run_inventory.py`: validates fallback timeout inventory. Optional lane command documented in `docs/09-Quality/root/testing.md`. |
| Testcontainers default-lane bleed-through | The shared-container strategy exists in `tests/integration/conftest.py`, but several integration modules still created their own testcontainers directly. Even when some fixtures were session-scoped, broad default runs could still start Docker reference stacks, duplicate services, and leave zombie containers after interruption. | optional-lane + process bug | Gate every direct testcontainers integration lane behind an explicit `COS_RUN_*` flag (`DATABASE_CONTAINERS`, `PLATFORM_SERVICES`, `OPIK_REFERENCE`, `COGNEE_REFERENCE`, `SMART_INFRA_CONTAINERS`, plus the app/e2e gates above). Add a contract test requiring future direct testcontainers files to declare and document an opt-in flag. | `tests/integration/test_databases.py tests/integration/test_platform_services.py tests/integration/test_opik_integration.py tests/integration/test_cognee_integration.py tests/integration/test_smart_infra_containers.py tests/integration/test_app_services.py tests/integration/test_e2e_flows.py tests/unit/test_test_run_inventory.py`: 5 passed, 98 skipped in 0.93s. |
| Non-Docker broad lane env contamination | The first broad non-Docker lane after Docker gating produced 11 failures, most of them from tests inheriting `COGNITIVE_OS_PROJECT_DIR` while still trying to exercise a temp Claude-style fixture. That made canonical-first behavior look broken even when the runtime was correctly preferring the canonical env. The same run also exposed that the p95 parser dropped valid `0 ms` hook-health samples because it used truthiness-based field extraction. | stale-fixture + active test harness bug | Make tests that validate temp installs/session hooks explicitly set `COGNITIVE_OS_PROJECT_DIR` to the temp project and clear competing driver env vars. Keep `record_completion` canonical-first by updating the test fixture, not the runtime. Add `CODEX_PROJECT_DIR` fallback to `ref_key_loader`, teach catalog sync to treat versioned `skills/` as a valid source-of-truth alongside generated `.cognitive-os/skills/`, and fix the p95 helper so zero-duration samples are preserved as performance evidence. Document the broad non-Docker lane as the default next repair queue before full-suite runs. | Broad lane evidence: `11 failed, 3756 passed, 1161 skipped, 124 deselected, 1 xfailed` before repair. Targeted repair lane: `tests/architecture/test_wiring.py::test_every_skill_in_catalog tests/contracts/test_p95_hook_latency.py tests/integration/test_e2e_self_repair.py::TestPipelineIntegrationFullChain::test_cost_events_written_by_main tests/integration/test_fresh_install_canary.py::TestFreshInstallCanary tests/integration/test_inject_phase_context_expansion.py::TestRefKeyExpansion::test_expansion_produces_adaptive_bypass_content tests/integration/test_post_commit_banner.py::TestSessionInitBanner`: 20 passed. |
| Working-dir cache timing and record-completion env isolation | The next broad non-Docker lane reduced the visible failures to two before timeout. `record_completion` still had additional subprocess fixtures using Claude-only project env, so the test passed/fail depended on inherited canonical env. `test_latency_under_50ms_cached` used wall-clock p95 deltas as a proxy for cache correctness, which is sensitive to process-launch jitter and can fail even when the cache works. | false-positive-risk + stale-fixture | Update the remaining record-completion subprocess fixtures to set canonical `COGNITIVE_OS_PROJECT_DIR` and clear competing driver env vars. Strengthen the working-dir cache test by instrumenting the fake `git` shim and proving warm/re-warmed cached invocations do not call `git worktree list`; keep latency as a sanity ceiling and median recovery check instead of the only proof. | Broad lane evidence after prior repair: `2 failed, 3891 passed, 1162 skipped, 124 deselected, 1 xfailed` before timeout. Targeted lane: `tests/integration/test_agent_working_dir_inject.py::test_latency_under_50ms_cached tests/integration/test_e2e_self_repair.py::TestPipelineIntegrationFullChain::test_cost_events_written_by_main tests/integration/test_e2e_self_repair.py::TestPipelineIntegrationFullChain::test_record_completion_main_via_subprocess`: 3 passed. Wider confirmation lane: `tests/integration/test_agent_working_dir_inject.py tests/integration/test_e2e_self_repair.py::TestPipelineIntegrationFullChain`: 12 passed. Follow-up broad non-Docker lane reached timeout with `0 failed, 3660 passed, 1160 skipped, 124 deselected, 1 xfailed`; remaining non-green status is timeout/skips, not a visible failure in the covered segment. |
| Post-SIGALRM full-run review findings | The first full run after replacing SIGALRM timeout fixtures reported `7 failed / 7405 passed / 28 skipped`. The failures matched items previously labeled "pre-existing", but review showed they were active product-contract drift: Codex init still created Claude driver rule/skill projections, hook registration checked stale script text instead of security-profile JSON, the pytest summary wrapper missed explicit xdist forms, and a removed Langfuse record-completion E2E lane remained after the Phoenix/OTel migration. | active-contract + stale-contract + false-positive-risk | Treat "pre-existing" as triage context only, not an exemption. Fix Codex init so non-Claude harnesses install skills/rules canonical-first without creating `.claude/`; point the hook-registration gate at generated security-profile JSON; make `pytest-with-summary.sh` honor every supported explicit xdist worker form; retire the leftover record_completion -> Langfuse E2E class and enforce the sunset through a contract test. | Focused validation added for canonical Codex install, pre-commit security profile source-of-truth, xdist wrapper args, and Langfuse sunset policy. |
| Broad non-Docker serial signal | A broad `tests/ -m "not docker"` run through `pytest-with-summary.sh` used to be silently parallelized by adaptive worker injection. That made the full-suite signal noisy: xdist worker death, shared-state races, and session timeout were mixed with real failures. The first serial rerun produced a smaller and more actionable queue: 23 failed before session timeout, dominated by stale fake COS sources after the `cos-init.sh` -> `cos_init.py` migration plus stale help/runtime-hardcoding/compose profile contracts. | active-contract + stale-fixture + process bug | Make broad/stateful lanes serial by default unless the caller passes explicit `-n` or `COS_PYTEST_WORKERS`; keep adaptive workers for unit-style lanes. Update ADR-068 and testing docs. Repair fake COS source fixtures to copy `scripts/cos_init.py`, update the `cos-init --help` audit contract, move runtime-hardcoding allowlist entries from the shell shim to `scripts/cos_init.py`, refresh hook scorecard counts, leave subprocess FD headroom in the FD chaos test, and align service-health compose profiles for Nemo/Jupyter. | Wrapper tests: `tests/unit/test_pytest_with_summary.py`: 6 passed. Repair lane: `tests/audit/test_install_scripts.py::test_help_flag_exits_cleanly tests/behavior/test_auto_update.py::TestCosInitRegistration::test_cos_init_registers_project tests/contracts/test_runtime_hardcoding.py::test_runtime_hardcoding_manifest_is_well_formed tests/chaos/test_fd_exhaustion.py::test_so_vitals_survives_fd_pressure tests/audit/test_hooks_contracts.py::test_hook_counts_match_scorecard tests/integration/test_auto_update_flow.py tests/integration/test_auto_update_safety.py tests/integration/test_service_health.py`: 72 passed, 7 skipped. |

## Passes Still Needing Trust Review

Passing tests remain subject to audit when they guard master-plan claims. Priority areas:

- Structural-only tests that assert file existence, headings, or catalog mentions without executing behavior.
- Tests that inspect `.claude/settings.json` only and ignore Codex/canonical `.cognitive-os` projections.
- Tests that pass because they skip optional dependencies silently.
- Tests that assert historical counts instead of deriving contracts from source of truth.
- Tests that validate docs wording but not runtime capability.

## Skips/Xfails Policy

Skips and xfails are not automatically acceptable. Each must be moved to one of:

- default lane: install/provide the dependency and run it locally;
- optional lane: explicitly documented command/environment with reason;
- active bug: remove skip/xfail by implementing the missing behavior;
- stale test: update or delete after historical confirmation.
