schema_version: policy-eval/v1
status: active
owner: platform-safety
adr: ADR-234

policy_dir: policies
mode: hook-migration
external_engines:
  opa: deferred
  cedar: deferred
  casbin: deferred

migrated_hooks:
  - hooks/destructive-git-blocker.sh
  - hooks/destructive-rm-blocker.sh
  - hooks/protected-config-write-guard.sh

active_policies:
  - policies/destructive-bash.yaml
  - policies/destructive-git.yaml
  - policies/protected-config-write.yaml
settings_projection:
  command: scripts/cos-policy-settings-projection
  mode: plan-only

invariants:
  - no_external_policy_engine_dependency
  - deny_or_block_wins
  - default_allow_in_slice_a
