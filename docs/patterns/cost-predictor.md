# Cost Predictor Pattern

The `cost-predictor` rule key refers to project-history-backed cost estimates
from `scripts/cost_predict.py` and `lib/cost_predictor.py`. Use it before large
or repeated model workloads so work can be decomposed or routed proportionally.
