# Cognitive OS — Python Dependencies
# Install: pip install -r requirements.txt

# === Core (always needed) ===
pyyaml>=6.0
jinja2>=3.1

# === Agent Communication Bus ===
redis>=5.0.0                 # Valkey/Redis client for pub/sub agent bus

# === Webhook Trigger ===
fastapi>=0.100
uvicorn>=0.23

# === Observability (ADOPT) ===
opik>=1.0                    # LLM tracing, evaluation, monitoring (cloud: comet.com)
mlflow>=2.0                  # MIGRATED FROM DOCKER: replaces langfuse-web + 5 dep containers. Run: mlflow server --backend-store-uri sqlite:///mlflow.db

# === Memory (ADOPT) ===
cognee>=0.1                  # Knowledge graph memory
# cognee[server]             # Uncomment to run as server

# === Guardrails (ADOPT) ===
nemoguardrails>=0.10         # MIGRATED FROM DOCKER: replaces nemo-guardrails container. Use RailsConfig + LLMRails in-process.

# === Jupyter / ML Compute (ADOPT) ===
jupyter>=1.0                 # MIGRATED FROM DOCKER: replaces jupyter/scipy-notebook container. Run: jupyter lab
notebook>=7.0                # Jupyter notebook interface

# === Web Crawling (ADOPT) ===
crawl4ai>=0.8.0                # LLM-ready web crawling (Apache 2.0, by UncleCode)

# === LLM Routing ===

# === Agent Testing (ADOPT) ===
deepeval>=1.0                # LLM unit testing, trajectory eval, 60+ metrics
ragas>=0.4                   # RAG quality testing, synthetic test generation

# === Agent Testing (TRIAL) ===
# promptfoo                  # Red teaming + prompt regression (Node.js CLI, install via npm)
# strands-agents-evals>=1.0  # Trace-based trajectory eval (OpenTelemetry)

# === Testing Infrastructure ===
testcontainers>=4.0          # Docker-based integration testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-timeout>=2.2
pytest-xdist>=3.5              # Parallel test execution (-n auto)
