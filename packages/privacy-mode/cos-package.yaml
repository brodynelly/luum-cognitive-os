name: "@luum/privacy-mode"
version: "1.1.1"
description: "Private mode protocol — disable all persistence for casual conversations"
authors: ["Cognitive OS Team"]
license: "Apache-2.0"
keywords: ["privacy", "private-mode", "persistence", "disable"]
cos_version: ">=0.1.0"
provides:
    - rule
exports:
    - source: "rules/private-mode.md"
      type: rule
      description: "Private mode activation/deactivation protocol"
      always_active: false
