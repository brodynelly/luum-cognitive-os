#!/usr/bin/env bash
# SCOPE: both
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
exec python3 "$PROJECT_DIR/scripts/primitive_closure_ratchet.py" "$@"
