# Dogfood Score Pattern

The `dogfood-score` rule key refers to the SO self-build maturity KPI produced
by `scripts/dogfood_score.py` and `lib/dogfood_scorer.py`. Use it when evaluating
whether Cognitive OS primitives are real, routed, tested, and improving under
actual self-use.
